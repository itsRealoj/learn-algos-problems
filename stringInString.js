/*
s is a small string and b is a bigger one, find all permutations of
s within b.
*/

function strings(s, b) {
    let str = '';
    let strObj = {}
    for(let char of b) {
        str += char;
        strObj
    }
    console.log(permutations)
    return permutations;
}
strings('new', 'newnamenewanfjdnew')